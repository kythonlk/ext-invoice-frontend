version: '3.8'

services:
  erp-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: erp-frontend
    ports:
      - "4221:80"
      - "3000:80"
    restart: unless-stopped
